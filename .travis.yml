language: java
services:
  - docker
jdk:
  - oraclejdk8
install:
  - ./gradlew assemble --stacktrace --info
script:
  - ./gradlew check integrationTest --stacktrace --info
after_success:
  - bash <(curl -s https://codecov.io/bash)